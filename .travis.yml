language: python
python:
  - "3.6"
install:
  - apt-get install -y default-jre
  - yarn install
  - yarn run test-db&; export DYNAMO_PID=$!
  - pip install pipenv
  - pipenv install --dev --system
script:
  - pytest --cov=. --ignore=vuemail-dark
after_script:
  - kill -9 $DYNAMO_PID